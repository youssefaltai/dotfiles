# Reckit (company) identity — applied only to repos under ~/work/reckit/.
# Only the differences from the personal base; name stays "Youssef".
[user]
	email = youssef@goreckit.com
	signingkey = ~/.ssh/id_ed25519_reckit.pub
