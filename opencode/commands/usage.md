---
description: >-
  Fuller usage breakdown than /cost — by model and by period, with anomalies
  called out and one actionable takeaway.
  Usage: /usage
---

Stats output:

!`opencode stats`

Give a fuller breakdown than /cost:
- Break down by model, and by period (day/week/total) if the data supports it.
- Call out anomalies: unusually expensive days, a model driving
  disproportionate cost, spikes vs the rest of the trend.
- Don't invent breakdowns the data can't support — if it's only totals, say
  that plainly instead of fabricating a per-model or per-day split.
- End with exactly one actionable observation (e.g. tasks/models that could
  move to a cheaper/smaller model, or a pattern worth watching).
- Keep it tight — tables over prose, no filler.
