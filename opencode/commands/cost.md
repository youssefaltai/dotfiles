---
description: >-
  Report cost and token usage for OpenCode from local stats — current day plus
  totals, one small table max.
  Usage: /cost
---

Stats output:

!`opencode stats`

Report cost and token usage concisely from the data above:
- Current day usage (cost + tokens) and all-time totals.
- At most one small table. No prose padding around it.
- If the stats can't isolate the current session specifically, say so plainly
  and report the nearest figure you do have (e.g. today's total) instead of
  guessing.
- Do not speculate about pricing, rates, or breakdowns that aren't present in
  the output — if a number isn't in the data, say it isn't available.
